import 'package:flutter/material.dart';

class AppColors {
  // Canlı Yeşil (Butonlar ve Birincil Eylemler)
  static const Color primary = Color(0xFF12A14B);
  
  // Marka Mavisi (Aktif Sekmeler ve Vurgu Alanları)
  static const Color brandBlue = Color(0xFF2563EB);
  
  // Arka Plan ve Kart Renkleri
  static const Color background = Color(0xFFF8FAFC);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color error = Color(0xFFEF4444);
  
  // Yazı Renkleri (Slate Skalası)
  static const Color textPrimary = Color(0xFF0F172A);   // Slate 900
  static const Color textSecondary = Color(0xFF64748B); // Slate 500
  static const Color border = Color(0xFFCBD5E1);        // Slate 300
}